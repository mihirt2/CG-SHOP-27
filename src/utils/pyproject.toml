[project]
name = "utils"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "benchexec>=3.35; sys_platform == 'linux'",
    "cgshop2027-pyutils",
    "pydantic>=2.13.5",
]

[tool.uv.sources]
cgshop2027-pyutils = { git = "https://github.com/CG-SHOP/pyutils27" }
